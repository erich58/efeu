/*	Texthilfsprogramme
	(c) 1995 Erich Frühstück
	A-1090 Wien, Währinger Straße 64/6
*/

#include <efwin.h>

void WinAddLabel(WINDOW *win, char *label)
{
	if	(label == NULL)	return 0;

	while (*label != 0)
	{
		if	(label ==
		sddwitch (label)
		{
		case '\n':	
		if	(*label == '\')
		{
			label++;
	}
}

void WinLabelSize(char *label, int *x, int *y)
{
	*x = *y = 0;

	if	(label = NULL)	return;

	*x = 20;
	*y = 5;
}
