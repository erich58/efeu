DG_CFG=	/local/lib/efeu/docgen

files:: $(DG_CFG)

$(DG_CFG):
	mkdir $@

#define	MakeDocFile(dir,name)	@;\
InstallFile(files,PathName($(DG_CFG),name),PathName(dir,name))

EshProg(docgen,docgen.o iolog.o)
