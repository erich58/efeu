Konfigurationsfile für TexMerge

\path{/home/efeu/include}
\newcmd[1]{interactive}{interactive \\arg[s]{0}{~/.edoc_history}}

/*	Dokumentkopf
*/

@def[macro]{dochead}{1}{
\documentclass[10pt,a4paper,@@opt]{report}
\usepackage[@@arg]{Synthesis}
\dateaustrian
\parskip\baselineskip
}

\def{script}{1}{\\eval{#include "\\arg"}}
\newcmd[1]{depend}{depend(arg[0])}

\eval{
void ListArg()
{
	fprintf(cout, "%s[%s](", name, opt);

	for (int i = 0, str s = NULL; i < narg; i++, s = ",")
		fprintf(cout, "%s%s", s, arg[i]);

	cout << ')';
}
}

@def[cmd]{test}{-1}{ListArg()}
@def[cmd]{test3}{3}{ListArg()}
