@name
@synopsis -h
\Description
The command |$!| converts lines ending with carriage-return (CR) and
linefeed (LF) to lines end­ ing with just linefeed, or vice versa.
The files are converted in place. If the files contains binary data or
contains already the right line ending, the original file is
untouched.

@arglist -i
@copyright -h
