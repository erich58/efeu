\secnum[3]
\entry{memswap}
\Name
\caption{Speicherinhalt austauschen}

\Synopsis
\tthang
#include <EFEU/memalloc.h>

void memswap(void *ap, void *bp, size_t n)
\end

\Description
Die Funktion \tt{memswap} vertauscht den Inhalt der Speicherfelder
beginnend bei \it{ap} und \it{bp}. Die Variable \it{n} gibt die Zahl der 
Bytes an, die vertauscht werden sollen.

\SeeAlso
\tt{alloctab(3)}, \tt{memalloc(3)}.\break
\tt{malloc(3S)} im \PRM.

\Warnings
Falls sich die beiden Speicherfelder überlappen, ist das Resultat
unbestimmt.
