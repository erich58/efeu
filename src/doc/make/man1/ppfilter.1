\Name
mkmfpost - Postfilter für C--Preprozessor
\Synopsis
\hang
|ppfilter| [~|-c|~]
\end

\Description
Das Kommando |mkmfpost| liest von der Standardeingabe und
schreibt zur Standardausgabe.
Es wird als Filter bei der automatischen
Generierung von Beschreibungsdateien für |make| verwendet.
Falls Option |-c| gesetzt
ist, wird bei der Ausgabe an jedem Zeilenvorschub ein
Wagenrücklauf angehängt.

Da das Kommando nicht direkt aufgerufen wird, wurde auf eine
Überprüfung der Argumentliste verzichtet.

\SeeAlso
\mref{make(1)}, \mref{mkmf(1)}.
